
import type { VotingCategory } from './types';

// This file is now empty as votingCategoriesData has been moved to voting-data.json
// It can be deleted if no other shared data is added here.
// For now, keeping the file structure but noting its content has moved.
